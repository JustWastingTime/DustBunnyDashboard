# Local backend only. Never prefix secrets with VITE_.
UMA_API_KEY=uma_k_replace_me
PORT=4174
DATABASE_PATH=./data/dashboard.sqlite

# Optional: publish sanitized input through the GitHub Contents API.
GITHUB_TOKEN=
GITHUB_OWNER=
GITHUB_REPO=
GITHUB_BRANCH=main
GITHUB_PUBLIC_INPUT_PATH=public-data/input.json
